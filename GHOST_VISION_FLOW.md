# Ghost Vision - Data Flow Diagram

## Phase 1: Baseline Creation with Ghost Frame Generation

```
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND: User uploads 5 baseline videos                          │
│  ├─ Video 1 (File object)                                          │
│  ├─ Video 2 (File object)                                          │
│  ├─ Video 3 (File object)                                          │
│  ├─ Video 4 (File object)                                          │
│  └─ Video 5 (File object)                                          │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  API: POST /api/create_baseline                                    │
│  └─ FormData with 5 videos + calibration params                    │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND: Process all 5 videos in parallel                         │
│  ├─ PoseEngine.process_video(video_1) → metrics_1                  │
│  ├─ PoseEngine.process_video(video_2) → metrics_2                  │
│  ├─ PoseEngine.process_video(video_3) → metrics_3                  │
│  ├─ PoseEngine.process_video(video_4) → metrics_4                  │
│  └─ PoseEngine.process_video(video_5) → metrics_5                  │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND: Calculate Baseline Statistics                            │
│  └─ PoseEngine.create_baseline_stats([m1, m2, m3, m4, m5])        │
│     ├─ Mean values for all metrics                                 │
│     ├─ StdDev values for all metrics                               │
│     ├─ Min/Max ranges                                              │
│     └─ Identify BEST video (minimum deviation from mean)           │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND: Generate Ghost Frames from Best Video                    │
│  └─ PoseEngine.generate_ghost_frames(best_video_path)              │
│     ├─ MediaPipe Pose with enable_segmentation=True                │
│     ├─ For each frame:                                             │
│     │  ├─ Extract segmentation mask                                │
│     │  ├─ Process mask → clean silhouette                          │
│     │  ├─ Create RGBA image (cyan silhouette + alpha)             │
│     │  └─ Save as ghost_frame_XXXXXX.png                           │
│     └─ Returns: {frames_processed, fps, dimensions}                │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND: Save Baseline + Ghost Metadata                           │
│  └─ baseline.json includes:                                         │
│     ├─ Baseline statistics (mean, std, ranges)                     │
│     ├─ ghost_frames: {total, fps, dimensions, best_video_index}    │
│     └─ viewType, speed, fps, created_at                            │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Receive Baseline Results                                │
│  └─ Response includes:                                              │
│     ├─ ghost_vision_available: true                                │
│     ├─ ghost_frames_count: N                                       │
│     └─ baselineRanges: {...}                                       │
└─────────────────────────────────────────────────────────────────────┘
```

## Phase 2: User Video Analysis with Ghost Vision

```
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND: User uploads running video                              │
│  └─ Video file + calibration params                                │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  API: POST /api/detect_anomaly                                     │
│  └─ FormData with video + params                                   │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND: Process User Video                                       │
│  ├─ PoseEngine.process_video(user_video) → user_metrics            │
│  ├─ PoseEngine.calculate_z_scores(user_metrics, baseline_stats)    │
│  └─ Generate skeleton_video with pose overlay                      │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Display Results in ResultsStep                          │
│  └─ VideoHolder shows:                                              │
│     ├─ skeleton_video (with pose overlay)                          │
│     └─ ResultsStep shows:                                           │
│        ├─ Metrics comparison                                       │
│        ├─ Z-scores                                                 │
│        └─ Ghost Vision Toggle (if available)                       │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼ (user enables Ghost Vision)
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Ghost Vision Enabled                                    │
│  └─ analysisStore.setGhostVision(true)                             │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  COMPONENT: GhostVisionOverlay mounted                             │
│  └─ Listens to video events:                                       │
│     ├─ play → start frame sync loop                               │
│     ├─ pause → stop updating                                       │
│     └─ seeked → update to new frame                               │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼ (video plays)
┌─────────────────────────────────────────────────────────────────────┐
│  COMPONENT: Frame Synchronization Loop                            │
│  └─ requestAnimationFrame → updateGhostFrame()                     │
│     ├─ frameNumber = floor(videoTime * fps)                        │
│     ├─ if (frameNumber != currentFrame):                           │
│     │  └─ Fetch new ghost frame                                    │
│     └─ else: reuse cached ghost image                             │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  API: GET /api/ghost_frame_by_number/{frameNumber}                 │
│  └─ Returns: {status, frame_number, ghost_url, filename}           │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  API: GET /api/ghost_frame/{filename}                              │
│  └─ Serves: ghost_frame_XXXXXX.png (RGBA PNG with transparency)    │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  COMPONENT: Draw Ghost on Canvas                                   │
│  └─ loadGhostImage(url) → Image object                             │
│     ├─ ghostCtx.globalAlpha = 0.5                                  │
│     ├─ ghostCtx.drawImage(ghost, 0, 0, canvas.width, canvas.height)│
│     └─ Result: Semi-transparent cyan silhouette overlay            │
└─────────────────────────────────────────────────────────────────────┘
```

## Visual Comparison Example

```
┌─────────────────────────────────────────────────────────────────────┐
│                    VIDEO PLAYBACK WITH GHOST VISION                 │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                                                             │  │
│  │    ╔══════════╗                                            │  │
│  │    ║  ACTUAL  ║  ← User's video (opaque, full color)      │  │
│  │    ║  VIDEO   ║                                            │  │
│  │    ║  RUNNER  ║       ╔═══════════╗                       │  │
│  │    ╚══════════╝       ║  GHOST    ║  ← Baseline silhouette│  │
│  │                       ║ BASELINE  ║     (50% cyan)         │  │
│  │                       ║ (Perfect) ║                        │  │
│  │                       ╚═══════════╝                        │  │
│  │                                                             │  │
│  │  User can see:                                              │  │
│  │  • Trunk lean difference (ghost vs actual)                 │  │
│  │  • Arm position deviation                                  │  │
│  │  • Foot strike pattern difference                          │  │
│  │  • Overall posture comparison                              │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  [◀◀] [▶] [▶▶] ━━━━━━━━━━●━━━━━━ 00:15 / 00:30                  │
│                                                                     │
│  👻 Ghost Vision: [ON] ← Toggle switch                             │
└─────────────────────────────────────────────────────────────────────┘
```

## Key Components Interaction

```
┌─────────────────────────────────────────────────────────────────────┐
│                         COMPONENT HIERARCHY                         │
│                                                                     │
│  App                                                                │
│   └─ StepHolder                                                     │
│      └─ ResultsStep ← Displays metrics + Ghost Vision toggle       │
│         └─ VideoHolder ← Displays skeleton video                   │
│            ├─ <video> ← Skeleton video element                     │
│            │   (bind:this={skeletonVideoElement})                  │
│            │                                                        │
│            └─ GhostVisionOverlay ← Canvas overlay component        │
│               (videoElement={skeletonVideoElement})                │
│               ├─ <canvas> ← Ghost rendering surface                │
│               └─ Frame sync logic                                  │
│                  ├─ Listens to video events                        │
│                  ├─ Fetches ghost frames                           │
│                  └─ Draws with opacity                             │
│                                                                     │
│  analysisStore ← Shared state                                      │
│   ├─ ghostVisionEnabled: boolean                                   │
│   └─ results: {ghost_vision_available, ...}                        │
└─────────────────────────────────────────────────────────────────────┘
```

## File Storage Structure

```
backend/
├── uploads/                    (temporary, cleaned after processing)
│   ├── baseline_video_1.mp4
│   ├── baseline_video_2.mp4
│   ├── ...
│   └── user_video.mp4
│
├── processed_videos/           (skeleton videos, served via API)
│   └── video_xyz_skeleton.mp4
│
├── ghost_frames/              (ghost silhouettes, served via API)
│   ├── ghost_frame_000000.png  ← Frame 0
│   ├── ghost_frame_000001.png  ← Frame 1
│   ├── ghost_frame_000002.png  ← Frame 2
│   ├── ...
│   └── ghost_frame_000899.png  ← Frame 899 (30fps × 30sec = 900 frames)
│
└── models/
    └── baseline.json           (includes ghost_frames metadata)
```

## Performance Considerations

```
┌─────────────────────────────────────────────────────────────────────┐
│  OPTIMIZATION STRATEGIES                                            │
│                                                                     │
│  1. Frame Caching                                                   │
│     ├─ Ghost frames cached on backend (static PNG files)           │
│     ├─ Loaded ghost images cached in memory (frontend)             │
│     └─ Only fetch when frame number changes                        │
│                                                                     │
│  2. Lazy Loading                                                    │
│     ├─ Ghost frames only fetched when Ghost Vision enabled         │
│     ├─ Frames loaded on-demand during playback                     │
│     └─ No preloading of all frames                                 │
│                                                                     │
│  3. Efficient Rendering                                             │
│     ├─ Canvas-based overlay (GPU accelerated)                      │
│     ├─ Only redraw when frame changes                              │
│     └─ requestAnimationFrame for smooth updates                    │
│                                                                     │
│  4. Graceful Degradation                                            │
│     ├─ Missing frames → no overlay (no error)                      │
│     ├─ Failed API call → log warning, continue                     │
│     └─ No segmentation → feature unavailable                       │
└─────────────────────────────────────────────────────────────────────┘
```

